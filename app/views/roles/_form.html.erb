<%= form_with(model: role) do |form| %>
  <% if role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
        <% role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :role_id, style: "display: block" %>
    <%= form.number_field :role_id %>
  </div>

  <div>
    <%= form.label :role_name, style: "display: block" %>
    <%= form.text_field :role_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
