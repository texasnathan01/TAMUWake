<%= form_with(model: set_role) do |form| %>
  <% if set_role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(set_role.errors.count, "error") %> prohibited this set_role from being saved:</h2>

      <ul>
        <% set_role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :role_id_id, style: "display: block" %>
    <%= form.text_field :role_id_id %>
  </div>

  <div>
    <%= form.label :user_id_id, style: "display: block" %>
    <%= form.text_field :user_id_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
