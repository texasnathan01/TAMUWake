<p style="color: green"><%= notice %></p>

<head>
	<%= stylesheet_link_tag "styles.css" %>
</head>

<ul>

  <a href="#"><banner>TAMU WAKE</banner></a>
  
  <a href="#"><p class="navbar navbarSettings">Settings</p></a>
  <a href="#"><p class="navbar currentPage"><%= link_to "Reciepts Index", receipts_path %></p></a>
  <a href="#"><p class="navbar"><%= link_to "Users index", users_path %></p></a>
  <a href="#"><p class="navbar">Account</p></a>
  <a href="#"><p class="navbar"><%= link_to "Wakeboard sets", wakeboard_sets_path %></p></a>
  <a href="#"><p class="navbar navbarHome">Home</p></a>
  
  <p class="bannerBox"></p>
  
  <h1> Treasury </h1>
  
  <%= link_to "New receipt", new_receipt_path %>
 
  <div id="stage_1">
  <h2> Pending Treasurer Approval (Stage 1)</h2>
  <% @receipts.where(stage: 1).each do |receipt| %>
      <p>
        <strong>Receipt ID:</strong>
        <%= receipt.id %>
        </br>
        <%= link_to "Show this receipt", receipt %>
      </p>

  <% end %>
  </div>
  
  <div id="stage_2">
    <h2> Approved By Treasurer, Awaiting Refund (Stage 2)</h2>
    <% @receipts.where(stage: 2).each do |receipt| %>
      <p>
        <strong>Receipt ID:</strong>
        <%= receipt.id %>
        </br>
        <%= link_to "Show this receipt", receipt %>
      </p>

    <% end %>
  </div>

  <div id="stage_3">
    <h2> Refund Received (Stage 3)</h2>
    <% @receipts.where(stage: 3).each do |receipt| %>
      <p>
        <strong>Receipt ID:</strong>
        <%= receipt.id %>
        </br>
        <%= link_to "Show this receipt", receipt %>
      </p>

    <% end %>
  </div>
</ul>